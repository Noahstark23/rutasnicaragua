# rutasnicaragua

Panel de administración web para gestionar rutas y paradas.

## Inicio rápido

```bash
# Backend
cd backend
npm install
node index.js

# Frontend
cd ../frontend
npm install
npm run dev
```

La clave de acceso se configura con `ADMIN_PASSWORD_HASH` en `backend/.env`.
